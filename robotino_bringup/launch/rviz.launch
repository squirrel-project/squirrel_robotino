<?xml version="1.0"?>
<launch>

  <arg name="robot" default="robotino"/>

    <!-- start rviz with configuration -->
    <node pkg="rviz" type="rviz" name="$(anon rviz)" args="-d $(find robotino_bringup)/robots/$(arg robot)/config/config.rviz" respawn="false" output="screen" />

</launch>

